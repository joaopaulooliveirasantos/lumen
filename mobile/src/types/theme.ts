export type ThemePalette = {
  appBackground: string;
  cardBackground: string;
  border: string;
  titleText: string;
  bodyText: string;
  mutedText: string;
  buttonText: string;
  buttonBackground: string;
  accent: string;
};
